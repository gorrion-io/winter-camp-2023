import Head from "next/head";
import styles from "../styles/Home.module.css";
import MergeVikingLists from "../lib/vikings";
import { Viking } from "../lib/vikings";
import VikingCard from "./VikingCard";

interface VikingProps {
  vikings: Viking[];
}

export default function Home(props: VikingProps) {
  return (
    <div className={styles.container}>
      <Head>
        <title>Dawid Listwon</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.main}>
        <ul className={styles.grid}>
          {props.vikings.map((viking: Viking) => {
            return (
              <li className={styles.card} key={viking.fullName}>
                <VikingCard viking={viking} />
              </li>
            );
          })}
        </ul>
      </main>
    </div>
  );
}

export async function getStaticProps() {
  return {
    props: { vikings: MergeVikingLists() },
  };
}
